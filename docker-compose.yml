services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: coursepilot
      POSTGRES_PASSWORD: cp_dev
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
